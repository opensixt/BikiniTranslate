{% extends "::layout.html.twig" %}

{% block title %}
    {{ 'groups'|trans }}
{% endblock %}

{% block body %}

    <h3>{{ 'groups'|trans }}</h3>

    <a href="{{ path('_admin_group_create') }}">{{ 'new_group'|trans }}</a>

    <table class="list">
        <thead>
            <tr>
                <th style="width:150px">{{ 'group'|trans }}</th>
                <th style="width:350px">{{ 'description'|trans }}</th>
            </tr>
        </thead>
        <tbody>
        {% for item in pagination %}
            <tr>
                <td><a href="{{ path('_admin_group', {'id': item.id}) }}">{{ item.name }}</a></td>
                <td>{{ item.description }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">{{ 'no_groups_found'|trans }}</td>
            </tr>
        {% endfor %}
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3">
                    {{ pagination.render()|raw }}
                </td>
            </tr>
        </tfoot>
    </table>

{% endblock %}