{% extends 'OpensixtBikiniTranslateBundle::base.html.twig' %}

{% block title %}
    {{ 'translation_tool'|trans }}
{% endblock %}

{% block body %}

    <h3>{{ 'translation_tool'|trans }}</h3>

    <form action="{{ path('_translate_setlocale') }}" method="post" {{ form_enctype(form) }}>
        {{ form_rest(form) }}
        <input class="btn" type="submit" value="{{ 'save_locale'|trans }}"/>
    </form>

{% endblock %}